# Business rules configuration
# These values can be overridden at runtime via environment variables without redeployment.

# Seconds since user creation before triggering an access alert notification.
user.access-alert.threshold-seconds=${ACCESS_ALERT_THRESHOLD_SECONDS:60}
